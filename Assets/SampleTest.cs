using UnityEngine;
using UnityEngine.UI;
using TamilEncoder;

public class SampleTest : MonoBehaviour
{
    [SerializeField] Text text;
    void Start()
    {
        //text.text =  TamilEncoding.Convert(text.text);
    }
}
